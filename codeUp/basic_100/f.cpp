#include <iostream>
using namespace std;

int main() {
    // 정수 두개 출력
    // int a,b;
    // scanf("%d %d", &a, &b);
    // printf("%d, %d", a, b);

    // 문자 두개 출력
    // char c,d;
    // scanf("%c %c", &c,&d);
    // printf("%c %c",d, c);

    //1015 - 소수점 이하 둘째 자리
    float a;
    scanf("%f", &a);
    printf("%.2f", a);
    return 0;
}